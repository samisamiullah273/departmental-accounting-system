# Notebook workflow

Use this folder for exploration and reporting notebooks. The application stores its SQLite database at `data/accounting.db`. A notebook can inspect approved transactions with Python's built-in `sqlite3` module, then import reusable services from `src/accounting.py`.